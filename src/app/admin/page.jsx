import AdminPanel from '@/components/AdminPanel';

export const metadata = { title: 'Pengelola', robots: { index: false, follow: false } };

export default function Admin() {
  return <AdminPanel />;
}
